/*cpp upend file if not exist then created new*/
#include <iostream>

#include <fstream>

#include <cstring>

using namespace std;

int writeOnfile(char* filetext) {
  fstream myfile;
  myfile.open("example.txt", fstream::app);
  myfile << filetext;
  myfile.close();
  return 0;

}
int test(char ttttt[]) {

  cout << ttttt << endl;
  writeOnfile(ttttt);
  return 0;
}
int main() {
  string init = "this";
  string add = " add";
  appendText.append(",");
  init.append(add);

  add = " two";
  init.append(",");
  init.append(add);

  add = " three";
  init.append(",");
  init.append(add);

  // writeOnfile(text);
  char cstr[ init.size() +1];
  strcpy(cstr, init.c_str());

  test(cstr);
  return 0;
}
